using semenarna;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace semenarna.Data.Repository {
    public interface IRepository {
        /*public bool AddProduct(int id);
        public bool GetProduct(int id);
        public bool RemoveProduct(int id);
        public bool UpdateProduct(int id);
        List<Post> GetAllProducts();
        List<Post> RemoveSelected(List<Post> posts);
        List<Post> AddProductList(List<Post> posts);
*/



    }
}
